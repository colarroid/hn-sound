/**
 * Names live here so a rename is one edit rather than a hunt through the app.
 * Nothing in this module imports React, so page metadata can use it freely.
 */
export const CHURCH_NAME = "The Hope Nation Church";
export const DEPARTMENT_NAME = "Sound & Technical";

export const APP_DESCRIPTION = `Internal platform for the ${DEPARTMENT_NAME} Department of ${CHURCH_NAME}.`;
